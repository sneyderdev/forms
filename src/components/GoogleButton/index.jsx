import React from 'react';

const GoogleButton = () => (
  <div>
    <form>
      <button type="button">
        <img src="" alt="" />
        Sign in with Google
      </button>
    </form>
  </div>
);

export default GoogleButton;
